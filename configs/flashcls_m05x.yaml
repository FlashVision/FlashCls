model:
  backbone_size: "0.5x"
  num_classes: 10
  input_size: [224, 224]
  dropout: 0.1

data:
  train_dir: data/custom/train
  val_dir: data/custom/val
  num_workers: 4

train:
  epochs: 150
  batch_size: 128
  learning_rate: 0.001
  save_dir: workspace/m05x
  label_smoothing: 0.1
